{
  "name": "orcabot",
  "version": "0.2.0",
  "description": "Orcamentista IA — SaaS de orçamento de obra com IA",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:web": "cd frontend && bun run dev",
    "dev:api": "uvicorn api.index:app --reload --port 3001",
    "build": "cd frontend && bun install && bun run build",
    "typecheck": "cd frontend && bun run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
